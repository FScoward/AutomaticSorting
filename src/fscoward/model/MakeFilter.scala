package fscoward.model
import java.nio.file.Path

class MakeFilter(dir: Path) {
  /*
   * ファイル名取得
   * takeFileName -> Array[String]
   * */
  def takeFileName = {
    
  }
  
  /*
   * 共通部分の取り出し(変動部分 第何回 とかの排除）
   * extraction -> 
   * */
  def extraction = {
    // コンペアとって共通部分を見る?
    
    
  }
  
  
}